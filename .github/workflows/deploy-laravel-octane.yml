name: Deploy Laravel Octane Container

on:
  push:
    branches: [main]
    paths:
      - 'apps/laravel-octane-container/**'
      - '.github/workflows/deploy-laravel-octane.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-laravel-octane
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: apps/laravel-octane-container
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '22'

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Install dependencies
        run: npm install

      - name: Type check
        run: npm run typecheck

      - name: Smoke-build Docker image
        run: docker build -t cloudflare-laravel-octane-demo:ci .

      - name: Deploy to Cloudflare (Workers + Containers)
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: apps/laravel-octane-container
          wranglerVersion: "4"
          command: deploy
